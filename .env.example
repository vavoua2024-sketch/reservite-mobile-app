# Adresse de votre backend RESERVITE (celui construit dans reservite-backend/).
# En local pendant le développement :
VITE_API_URL="http://localhost:4000"

# Une fois le backend déployé en ligne, remplacez par sa vraie adresse, ex:
# VITE_API_URL="https://reservite-api.onrender.com"
